── AI 诊断 (auto, 13轮) ──
基于已采集的数据，当前情况如下：

## 关键发现

| 指标 | 数据 | 来源工具 |
|------|------|---------|
| 实例运行时间 | **2 分钟** | health |
| 缓存命中率 | 99.34% | health |
| 活跃会话 | 4 个后台进程，无业务等待 | activesessions |
| OS 负载 | 0.06（极低） | os |
| 慢 SQL | 0 条 | slowsql |
| 表膨胀 | 存在 2 个膨胀表 | bloat |
| XID 年龄 | 2 个数据库需关注 | xid |
| Vacuum 状态 | 2 个表有 dead tuples | vacuum |

## 核心问题：数据库刚重启，处于冷启动状态

**当前无法直接诊断"越来越慢"的根因**，因为：
1. 数据库刚启动 2 分钟，Buffer Pool 是冷的
2. 没有业务会话在执行，无等待事件
3. 所有历史性能数据随重启丢失

**最可能的根因**：冷启动导致 Buffer Pool 未预热，查询需要从磁盘读取数据，性能下降。

---

## 需要你提供的信息

1. **重启原因**：是你主动重启还是数据库崩溃？崩溃的话可能有崩溃前的日志
2. **慢的具体表现**：哪些查询慢？平均响应时间从多少变到多少？
3. **是否每次重启后都慢**：还是这次特别慢？

---

## 紧急措施：Buffer Pool 预热

如果是冷启动问题，可以预热热点表：

```sql
-- 方案1：预热热点表（需要先确认表名）
-- SELECT pg_prewarm('表名');

-- 方案2：手动扫描热点表加载数据到内存
-- SELECT count(*) FROM 热点表;
```

需要你提供热点表名，或者告诉我业务主要访问哪些表，我可以帮你确认后给出完整的预热方案。

---

**请回答上述问题，我才能进一步定位根因。**当前数据不足以判断"越来越慢"是渐进性问题还是单次冷启动问题。

