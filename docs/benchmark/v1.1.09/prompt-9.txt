── AI 诊断 (auto, 5轮) ──
## 诊断结论

基于已采集数据，**当前数据库层面未发现性能瓶颈**：

| 指标 | 数据 | 来源 |
|------|------|------|
| CPU 负载 | LOAD = 0，IDLE_TIME 705 万 vs BUSY_TIME 7029 | os |
| 活跃会话 | 4 个后台线程，无用户业务会话 | activesessions |
| 等待事件 | 无，仅后台 WLM 线程 On CPU | waits / ash |
| 慢 SQL | 0 条（>1000ms） | slowsql |
| Top SQL 总耗时 | 0.20 秒（147 次调用） | topsql |
| 锁等待 | 无 | locks |
| 长事务 | 无 | longtx |
| 缓存命中率 | 99.32% | health |
| 连接数 | 12 / 200 (6%) | health |

---

## 分析

**数据库状态**：空闲健康，所有 17 项健康检查通过，TPS 31.7，无阻塞、无等待、无慢查询。

**CPU 使用率矛盾**：用户反馈"CPU 在涨"，但 OS 指标显示：
- LOAD = 0
- IDLE_TIME : BUSY_TIME ≈ 1000 : 1
- IOWAIT_TIME = 1889（有少量 IO 等待，但占比极低）

可能原因：
1. **CPU 消耗在数据库外部** — 其他进程（应用服务器、备份任务、日志采集等）占 CPU，数据库本身空闲
2. **监控采样的是瞬时值** — 用户看到的 CPU 峰值可能是短暂 spikes，当前已回落
3. **问题已消失** — 启动实例仅 1 小时 13 分钟，可能是启动时 WLM 采集等初始化导致的短暂 CPU 消耗

---

## 建议

当前数据库无异常。若问题仍在持续：

1. **检查宿主机其他进程**：
   ```sql
   -- 在数据库宿主机执行
   top -c
   ps aux --sort=-%cpu | head -20
   ```

2. **确认监控源** — 用户看到的 CPU 指标来自哪里？（云监控 / top / htop）

3. **等待问题复现时再采集** — 如果是间歇性问题，需在问题发生时重新诊断

